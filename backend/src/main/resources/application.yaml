spring:
  application:
    name: DuckDuck
  datasource:
    url: jdbc:mysql://localhost:3307/dev?serverTimezone=Asia/Seoul
    username: root
    password: qndnfrud104
    driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
      hibernate:
        ddl-auto: update # 테이블이 이미 생성되어 있다면 validate 권장
      show-sql: true
      properties:
        hibernate:
          format_sql: true

    security:
      oauth2:
        client:
          registration:
            kakao:
              client-id: 3d8a8e8648c42f40348bc423f47f7e36    # REST API 키
              client-secret: xY9GIG2sOde409IVrkSVj6QGqNpS5JK8 # 보안 키
              client-authentication-method: client_secret_post
              authorization-grant-type: authorization_code
              redirect-uri: "http://localhost:8080/login/oauth2/code/kakao"
              scope:
                - profile_nickname
                - account_email
              client-name: Kakao
          provider:
            kakao:
              authorization-uri: https://kauth.kakao.com/oauth/authorize
              token-uri: https://kauth.kakao.com/oauth/token
              user-info-uri: https://kapi.kakao.com/v2/user/me
              user-name-attribute: id # 카카오 응답에서 고유 식별자인 'id'를 사용